export const minTime = 1 * 60;
export const maxTime = 60 * 60;

export const defaultState = {
	breakLength: 5 * 60,
	sessionLength: 25 * 60,
	sessionTimer: 25 * 60,
	breakTimer: 5 * 60,
	isRunning: false,
};
